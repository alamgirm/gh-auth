server:
  port: 8080

spring:
  application:
    name: github-device-flow-backend
  
  # Database configuration (H2 for simplicity, can use PostgreSQL in production)
  datasource:
    url: jdbc:h2:mem:authdb
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  
  # H2 Console (for debugging)
  h2:
    console:
      enabled: true
      path: /h2-console

github:
  app:
    client-id: ${GITHUB_CLIENT_ID:Iv23liqRwUp1z3IgnKe3}
    client-secret: ${GITHUB_CLIENT_SECRET:}
    authorize-url: https://github.com/login/oauth/authorize
    token-url: https://github.com/login/oauth/access_token
    user-api-url: https://api.github.com/user
    redirect-uri: ${GITHUB_REDIRECT_URI:http://localhost:3000/auth/callback}

# Azure Entra ID (formerly Azure AD) configuration
azure:
  entra:
    client-id: ${BE_CLIENT_ID:5640aba4-729d-4e6f-a34d-86beccf78df5}
    tenant-id: ${BE_TENANT_ID:a95973d1-dd8e-4baa-af11-d18f9863793b}
    authority: ${AZURE_AUTHORITY:https://login.microsoftonline.com/${AZURE_TENANT_ID:common}}
    jwks-uri: ${AZURE_JWKS_URI:https://login.microsoftonline.com/${AZURE_TENANT_ID:common}/discovery/v2.0/keys}
    issuer: ${AZURE_ISSUER:https://login.microsoftonline.com/${AZURE_TENANT_ID:common}/v2.0}
    expected-audience: ${AZURE_EXPECTED_AUDIENCE:api://${AZURE_CLIENT_ID}}

# CORS configuration
cors:
  allowed-origins: http://localhost:3000
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

logging:
  level:
    root: INFO
    com.github.deviceflow: DEBUG

